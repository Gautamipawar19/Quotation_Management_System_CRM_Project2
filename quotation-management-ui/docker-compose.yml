version: "3.9"

services:
  frontend:
    build: .
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    image: quotation-backend-image
    ports:
      - "5000:5000"

